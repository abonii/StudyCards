<?xml version="1.0" encoding="UTF-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="item"
            type="abm.co.studycards.data.model.vocabulary.Category" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/holder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/layout_item_margin"
        android:clickable="true"
        android:focusable="true"
        app:cardElevation="0dp"
        app:cardBackgroundColor="@android:color/transparent"
        app:rippleColor="#10000000">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <androidx.cardview.widget.CardView
                android:id="@+id/outer_card"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:cardCornerRadius="@dimen/default_8dp"
                app:cardElevation="0dp">

                <androidx.cardview.widget.CardView
                    android:id="@+id/card"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    app:cardCornerRadius="@dimen/default_8dp"
                    app:cardElevation="@dimen/default_2dp">

                    <ImageView
                        android:id="@+id/image"
                        android:layout_width="@dimen/default_140dp"
                        android:layout_height="@dimen/default_200dp"
                        android:scaleType="centerCrop"
                        app:imageWithGlide="@{item.imageUrl}"
                        android:contentDescription="@string/just_image" />

                </androidx.cardview.widget.CardView>
            </androidx.cardview.widget.CardView>

            <TextView
                android:id="@+id/title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:gravity="top|center"
                android:maxLines="2"
                android:lines="2"
                android:text="@{item.mainName}"
                android:textColor="@color/textColor"
                android:textSize="@dimen/default_13sp"
                android:fontFamily="@font/helvetica_bold"
                tools:text="The best title" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>
